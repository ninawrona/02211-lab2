defproc twoWayMux (chan?(int<8>) A, B; chan?(bool) sel; chan!(int<8>) out)
{
  int a, b;
  bool s;
  chp {
    *[ sel?s;              // Wait for the selection token
       [ s ->  B?b; out!b
       [] ~s -> A?a; out!a
       ]
    ]
  }
}
